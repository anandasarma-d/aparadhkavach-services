package dev.aparadhkavach.orchestration.query.conversation;

/** Cited neighbor snapshot for follow-up resolution (mvp2/12 Step B). */
public record RelatedEntityRef(String id, String type, String label) {}
